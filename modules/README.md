# Modules

Feature modules will live here as the modular-monolith implementation grows.
This root maps to the domain-module concept in the build brief while keeping
the initial scaffold free of business features.
