# Providers

Provider adapters for AI, speech-to-text, storage, jobs, email, billing,
telemetry, and Microsoft Graph will live under this root when those stages are
implemented. No external provider SDKs are wired in this scaffold.
